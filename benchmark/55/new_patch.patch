--- ./benchmark/55/test2.cc	2024-03-10 18:24:11
+++ ./benchmark/55/test2_.cc	2024-03-10 18:24:29
@@ -966,7 +966,9 @@ class V8_EXPORT_PRIVATE Assembler : public AssemblerBa
   bool MustUseReg(RelocInfo::Mode rmode);
 
   // Record reloc info for current pc_.
-  void RecordRelocInfo(RelocInfo::Mode rmode, intptr_t data = 0);
+  
+void LoopHeaderAlign() { CodeTargetAlign(); }
+void RecordRelocInfo(RelocInfo::Mode rmode, intptr_t data = 0);
 
   // Block the emission of the trampoline pool before pc_offset.
   void BlockTrampolinePoolBefore(int pc_offset) {
@@ -1258,8 +1260,12 @@ class V8_EXPORT_PRIVATE Assembler : public AssemblerBa
   friend class RegExpMacroAssemblerRISCV;
   friend class RelocInfo;
   friend class BlockTrampolinePoolScope;
-  friend class EnsureSpace;
-  friend class ConstantPool;
+  friend 
+   
+class EnsureSpace;
+
+friend
+class ConstantPool;
 };
 
 class EnsureSpace {
